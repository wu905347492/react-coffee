import React from 'react';
import { Layout } from 'antd';
const { Header } = Layout;

const headerStyle = {
  padding: 0,
};

const Headers = () => {
  return <Header style={headerStyle}>11</Header>;
};

export { Headers };
